# RHEL 9 packages installed on top of Driver Toolkit (one per line; # comments ignored)
# Only packages in BaseOS / AppStream — no EPEL, Extensions, or CRB-only packages.
#
# Removed vs old Fedora toolbox:
#   bridge-utils — not in RHEL 9 (use iproute: ip link / bridge)
#   iftop        — Red Hat Extensions repo only (disabled by default)
#   htop         — not in RHEL 9 repos (EPEL); use top / bpftrace / perf

# eBPF / tracing
bpftrace
bcc-tools
bpftool
perf
strace
ltrace
blktrace

# Network capture & analysis
tcpdump
wireshark-cli
conntrack-tools

# IP / DNS / routing
iproute
iputils
bind-utils
net-tools
ethtool

# Network testing
curl
wget
nmap
nmap-ncat
iperf3
socat
traceroute
telnet
mtr

# Process / system
procps-ng
psmisc
sysstat
lsof
pciutils
iotop

# Storage / disk
gdisk
hdparm
smartmontools
nvme-cli

# Shell / utilities
bash-completion
vim-enhanced
less
