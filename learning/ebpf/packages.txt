# RHEL 9 packages on Driver Toolkit — BaseOS / AppStream only (no EPEL / Extensions).
# Groups: @group ebpf first, then @group tools (separate dnf layers in build.sh).
#
# Removed vs Fedora toolbox:
#   bridge-utils  — not in RHEL 9 (use iproute)
#   iftop         — Extensions repo only
#   htop          — EPEL only
#   wireshark-cli — conflicts with bcc-tools LLVM deps; use tcpdump
#   nmap-ncat     — provided by nmap
#   vim-enhanced  — DTK includes vim-minimal
#   curl          — DTK includes curl-minimal (/usr/bin/curl); full curl conflicts

# @group ebpf
bpftrace
bcc-tools
bpftool
perf
strace
ltrace
blktrace

# @group tools
tcpdump
conntrack-tools
iproute
iputils
bind-utils
net-tools
ethtool
wget
nmap
iperf3
socat
traceroute
telnet
mtr
procps-ng
psmisc
sysstat
lsof
pciutils
iotop
gdisk
hdparm
smartmontools
nvme-cli
bash-completion
less
